# autostand-app (Tauri)

Tauri v2 desktop app (Rust backend + React/Vite frontend) for autostand — cross-platform daily standup automation.

See the root [`README`](../../../README.md) and [`docs/`](../../../docs/) for the full architecture.