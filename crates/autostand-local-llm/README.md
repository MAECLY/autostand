# autostand-local-llm

Process-isolated JSONL sidecar used by Autostand's built-in local AI provider.
It delegates inference to the platform-specific `llama-cli` binary distributed
with desktop release bundles.
